package titans.roads;

import titans.geometry.Vector2d;

// not sure if it should have anything else
public class ConstraintSetXY {
    public Vector2d maxVel = new Vector2d(20, 20);
    public Vector2d maxAccel = new Vector2d(30, 30);
}
